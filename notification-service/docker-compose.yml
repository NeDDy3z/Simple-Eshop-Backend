services:
  notification-app:
    build: .
    container_name: notification-app
    ports:
      - "8083:8083"
    environment:
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
    networks:
      - eshop-net

networks:
  eshop-net:
    external: true
